spring:
  datasource:
    url: ${LOCAL_DB_URL:jdbc:postgresql://localhost:5432/budget_control_db}
    username: ${LOCAL_DB_USERNAME:admin}
    password: ${LOCAL_DB_PASSWORD:admin}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

cookie:
  secure: false
  same-site: Lax

#  sql:
#    init:
#      mode: always
#      data-locations: classpath:exercises_relational_full.sql
#      encoding: UTF-8